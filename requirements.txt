requests==2.31.0
pytest==7.4.3
allure-pytest==2.13.2
PyYAML==6.0
pytest-xdist==3.5.0          # 并行执行（可选）
deepdiff==6.7.1              # JSON 深度比较
pymysql==1.1.0               # MySQL 支持（可选）         # MySQL 支持（可选）
pytest-json-report>=1.5.0  # [NEW] 生成 JSON 报告便于统计
pymysql>=1.1.0        # 数据库支持
colorama>=0.4.6       # 彩色输出