一、设计思想
该框架遵循以下核心原则：

原则	实现方式
分层解耦	配置、数据、公共模块、测试用例、报告生成各自独立
数据驱动	测试数据使用 YAML 存储，与测试逻辑分离
关键字驱动	通过 run.py 传入不同参数，实现单接口/流程/标记/关键字/并行等多种执行方式
多环境支持	通过 config/settings.yaml 配置 dev/test/prod 三套环境，一键切换
多认证方式	支持 API Key、Bearer Token、Basic Auth
失败重试	网络请求自动重试（可配置次数）
详细日志	彩色控制台 + 文件日志，记录请求/响应/重试/断言
炫酷报告	使用 Allure 生成带图表、步骤、历史趋势的专业报告
高可扩展	轻松增加新的数据文件、测试用例、公共方法、数据库校验等

二、目录结构
text
perfect_api_test/
├── common/                     # 公共核心模块
│   ├── api_client.py           # HTTP客户端（认证、重试、Allure步骤）
│   ├── logger.py               # 彩色日志 + 文件轮转
│   └── database.py             # 数据库查询工具（可选）
├── config/                     # 配置管理
│   └── settings.yaml           # 多环境配置（dev/test/prod）
├── data/                       # 测试数据（YAML）
│   ├── api/                    # 单接口测试数据
│   │   └── issue_api.yaml
│   └── scenario/               # 业务流程测试数据
│       └── issue_flow.yaml
├── testcases/                  # 测试用例层
│   ├── conftest.py             # pytest 夹具（fixture）
│   ├── api/                    # 单接口测试用例
│   │   └── test_issue_api.py
│   └── scenario/               # 业务流程测试用例
│       └── test_issue_flow.py
├── utils/                      # 工具函数
│   ├── data_loader.py          # 加载 YAML 数据
│   └── assert_utils.py         # 增强断言（JSON深度比较）
├── reports/                    # 测试报告目录（自动生成）
│   ├── allure_results/         # Allure 原始数据
│   └── allure_html/            # Allure HTML 报告
├── logs/                       # 日志目录（自动生成）
│   └── api_test.log
├── requirements.txt            # 依赖清单
├── pytest.ini                  # pytest 配置（markers、addopts等）
├── run.py                      # 一键执行入口（支持多参数）
└── README.md                   # 项目说明

三、核心模块详解
1. common/api_client.py —— HTTP 客户端
功能：
封装 requests.Session，支持 API Key、Bearer Token、Basic Auth 三种认证方式。
自动重试（失败后指数退避等待）。
记录详细的请求/响应日志（请求 URL、参数、JSON 体，响应状态码、耗时、返回体）。
使用 @allure.step 将每次 HTTP 调用加入 Allure 报告。
关键方法：
class APIClient:
    def get(path, params=None)   # GET 请求
    def post(path, json=None)    # POST 请求
    def put(path, json=None)     # PUT 请求
    def delete(path)             # DELETE 请求
配置示例（在 settings.yaml 中）：

yaml
auth_type: "api_key"
auth_config:
  key: "X-Redmine-API-Key"
  value: "your_api_key"
2. common/logger.py —— 日志模块
功能：
同时输出到控制台（带颜色）和文件（自动轮转，单文件 10MB，保留 5 个）。
日志级别可配置（INFO/DEBUG/WARNING/ERROR）。
在 api_client 中自动调用，记录所有网络交互。
使用示例：
from common.logger import logger
logger.info("开始执行测试")
logger.debug(f"响应数据: {resp.text}")
3. common/database.py —— 数据库工具（可选）
功能：
简单封装 MySQL 查询（可扩展其他数据库）。
用于接口测试后校验数据库数据一致性。
注意：如果不需要数据库校验，可以不安装 pymysql 并删除相关调用。

4. utils/data_loader.py —— 数据加载器
功能：
加载 data/ 目录下的 YAML 文件。
返回指定 key 的数据列表，供 @pytest.mark.parametrize 使用。

示例：
test_data = get_test_data("api/issue_api.yaml", "issue_api_tests")
# 返回 YAML 中 issue_api_tests 对应的列表
5. utils/assert_utils.py —— 增强断言
功能：

assert_status_code：断言 HTTP 状态码，并附加到 Allure 报告。

assert_json_equal：深度比较两个 JSON（可忽略数组顺序），使用 deepdiff 库，差异会作为附件加入报告。

6. testcases/conftest.py —— Pytest 夹具
功能：

加载 settings.yaml 中 active 指定的环境配置。

提供 api_client 夹具（会话级别），自动创建认证客户端。

提供 test_context 夹具（函数级别），用于在用例间传递临时变量（如创建的 issue ID）。

7. testcases/api/ 和 testcases/scenario/ —— 测试用例
单接口测试：

每个接口独立测试，关注参数、响应、边界值等。

使用 @pytest.mark.parametrize 从 YAML 加载数据。

标记 @pytest.mark.api。

业务流程测试：

模拟用户完整操作（如 创建 → 更新 → 验证 → 清理）。

不强制使用 YAML 数据，也可硬编码，但建议将可变数据放入 YAML。

标记 @pytest.mark.scenario。

8. run.py —— 执行入口
功能：

解析命令行参数，支持：

--env：选择环境（dev/test/prod）

--api 或 --scenario：只执行指定类型

-m：执行指定标记（smoke/regression/api/scenario）

-k：按名称关键字过滤

-n：并行执行（需 pytest-xdist）

动态构建 pytest 命令。

执行完成后自动调用 allure generate 生成 HTML 报告，并打印报告路径。

四、配置文件详解 (config/settings.yaml)
yaml
active: "test"          # 当前激活的环境

environments:
  dev:
    base_url: "http://redmine-dev.llxzu.com"
    auth_type: "api_key"
    auth_config:
      key: "X-Redmine-API-Key"
      value: "dev_key"
    timeout: 30
    max_retries: 2
    database:   # 可选，如不需要可删除
      host: "localhost"
      port: 3306
      user: "dev_user"
      password: "dev_pass"
      database: "redmine_dev"

  test:
    base_url: "http://redmine.llxzu.com"
    auth_type: "api_key"
    auth_config:
      key: "X-Redmine-API-Key"
      value: "d88fad3f79bf84fbba694d80f4df7b9250671234"
    timeout: 15
    max_retries: 3

  prod:
    base_url: "https://redmine.prod.com"
    auth_type: "bearer"
    auth_config:
      token: "prod_token"
    timeout: 10
    max_retries: 1
切换环境：

修改 active 字段的值，或通过 --env 参数覆盖。

五、测试数据示例 (data/api/issue_api.yaml)
yaml
issue_api_tests:
  - name: "正常获取问题"
    issue_id: 32364
    expected_status: 200
    expected_fields:
      - id
      - subject
      - status
  - name: "不存在的ID"
    issue_id: 999999
    expected_status: 404
注意：

YAML 支持注释、复杂嵌套、列表、字典。

一个文件可以包含多个测试数据集（用不同 key 区分）。

六、编写测试用例
单接口测试用例模板
python
import allure
import pytest
from utils.data_loader import get_test_data
from utils.assert_utils import assert_status_code

@allure.feature("模块名")
class TestAPI:

    @allure.story("接口名")
    @pytest.mark.api
    @pytest.mark.smoke
    @pytest.mark.parametrize("case", get_test_data("api/xxx.yaml", "data_key"))
    def test_something(self, api_client, case):
        resp = api_client.get(f"/issues/{case['issue_id']}.json")
        assert_status_code(resp.status_code, case['expected_status'])
        # 更多断言...
业务流程测试用例模板
python
import allure
import pytest

@allure.feature("业务流程")
class TestFlow:

    @allure.story("完整生命周期")
    @pytest.mark.scenario
    def test_full_flow(self, api_client, test_context):
        # 1. 创建
        resp = api_client.post("/issues.json", json=create_payload)
        issue_id = resp.json()['issue']['id']
        test_context['issue_id'] = issue_id
        # 2. 更新状态
        # 3. 验证
        # 4. 清理
七、执行测试命令
基本执行
命令	说明
python run.py	全部测试（默认 test 环境）
python run.py --env dev	切换到 dev 环境
python run.py --api	只执行单接口测试
python run.py --scenario	只执行业务流程测试
python run.py -m smoke	执行标记为 smoke 的用例
python run.py -k "test_get"	执行名称包含 test_get 的用例
python run.py -n 4	4 个进程并行执行
python run.py -n auto	自动 CPU 核心数并行
组合示例
bash
# 在 test 环境执行所有冒烟用例
python run.py --env test -m smoke

# 只执行单接口中的部分用例（按关键字）
python run.py --api -k "issue"

# 并行执行所有流程用例
python run.py --scenario -n 2
八、查看测试报告
执行完 run.py 后，会自动生成 Allure 报告。终端会输出类似：

text
报告已生成: file:///home/user/perfect_api_test/reports/allure_html/index.html
用浏览器打开该文件即可。

Allure 报告内容：
概览：通过/失败/错误/跳过数量，测试时长，环境信息。

类别：按失败类型分组。

测试套件：按 @allure.feature 和 @allure.story 组织的树形结构。

每个用例：包含步骤、参数、日志、附件（如断言的差异）。

趋势：历史执行对比（需要 Jenkins 集成）。

九、扩展与集成
1. 添加新的测试模块
在 data/ 下新增对应的 YAML 文件。

在 testcases/api/ 或 testcases/scenario/ 下新建 Python 文件，复制模板并修改。

2. 数据库校验
在 conftest.py 中添加 db_client 夹具，从 settings.yaml 读取数据库配置。

在用例中调用 db_client.query_one() 验证数据落库。

3. 增加自定义认证方式
修改 api_client.py 的 __init__ 方法，添加新的 auth_type 分支。

4. 集成 Jenkins
安装 Allure Jenkins Plugin。

在构建脚本中执行 python run.py --env test --api。

构建后步骤选择 “Allure Report”，目录填写 reports/allure_results。

每次构建会自动生成并发布报告。

5. 定时执行
使用 Jenkins 定时触发器（如每天凌晨）。

或使用 Linux crontab：0 2 * * * cd /path/to/project && python run.py

十、常见问题解答
Q1: 运行 run.py 报错 allure: command not found
A: 需要安装 Allure 命令行工具。下载地址：https://github.com/allure-framework/allure2/releases ，解压后将 bin 目录加入 PATH。

Q2: 如何跳过某些用例？
A: 使用 @pytest.mark.skip 装饰器，或在 pytest.ini 中配置 -k 排除。

Q3: 测试数据中可以用环境变量吗？
A: 可以，在 data_loader.py 中添加 os.getenv() 替换占位符，或使用 pyyaml 的 !include 语法。

Q4: 并行执行时报告会混乱吗？
A: Allure 支持并行执行，pytest-xdist 会自动合并结果。

Q5: 如何调试单个用例？
A: 在 PyCharm 中直接运行测试函数，或使用 pdb 断点。